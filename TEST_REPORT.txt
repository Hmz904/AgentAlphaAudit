AgentAlphaAudit v0.5 local validation (2026-09-11)

- python -m compileall -q src scripts tests: PASS
- ./scripts/run_demo.sh: PASS
- pytest -q -rs: 32 passed, 4 skipped in 1.71s

New v0.5 mandatory coverage includes:
- exact workspace-config hashing/reconciliation before qrun;
- fail-closed discovery of unknown future template families containing segments;
- holdout_access=no requires static preflight + observed runtime-config match + physical provider truncation;
- mandatory fake-runtime test proves the runtime_qlib_config event is tied to the active running trial.

Skipped locally because this sandbox does not have the optional rdagent package installed:
- tests/test_config_audit_rdagent_real_optional.py: installed-template contract;
- tests/test_config_audit_rdagent_real_optional.py: real QlibFBWorkspace injected-workspace config contract;
- tests/test_rdagent_real_contract_optional.py: two real RD-Agent 0.8.0 feedback/experiment contracts.

An attempt to pip-install rdagent==0.8.0 in this container failed because outbound package-network access is unavailable. CI job `rdagent-080-contract` installs the pinned optional dependency and runs the full suite.

ruff is not installed in this offline container, so no local ruff claim is made in this report.
